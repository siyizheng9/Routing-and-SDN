# Routing-and-SDN

solution for the course routing and SDN.

Directory `./ip_exercise`:

Includes shell scripts and configuration files for implementing RIP, OSPF and BGP routing between LXC containers using Quagga.

Directory `./SDN`:

A Ryu application implements basic layer2 switch, vlan and firewall function as well as a vlan management interface exposed with simple REST API.

